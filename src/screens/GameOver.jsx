import React from 'react';

export default function GameOver({ onRestart }) {
  return (
    <div style={{
      width: '100%',
      height: '100vh',
      background: '#0a0f1e',
    }}>
    </div>
  );
}
